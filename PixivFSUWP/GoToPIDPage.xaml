<Page
    x:Class="PixivFSUWP.GoToPIDPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PixivFSUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="500">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Pixiv ID:" HorizontalTextAlignment="Right" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="tbPID" TextChanged="tbPID_TextChanged"/>
            <Button Grid.Column="2" VerticalAlignment="Stretch" Click="BtnGoTo_Click">
                <Button.Content>
                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" 
                                  FontSize="16" Glyph="&#xE724;"/>
                </Button.Content>
            </Button>
        </Grid>

    </Grid>
</Page>
