<Page
    x:Class="PixivFSUWP.ReportIssuePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PixivFSUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Vertical">
            <TextBlock x:Uid="Report"/>
            <TextBlock/>
        </StackPanel>
        
        <ScrollViewer  Background="{ThemeResource SystemChromeDisabledHighColor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                          HorizontalScrollMode="Disabled" VerticalScrollBarVisibility="Visible" Grid.Row="1">
            <TextBlock Margin="5" x:Name="txtDetails"  TextWrapping="Wrap" HorizontalAlignment="Stretch"/>
        </ScrollViewer>
        
        <StackPanel Orientation="Vertical" Grid.Row="2">
            <HyperlinkButton x:Uid="Copy" x:Name="btnCopy" Grid.Row="2" Click="BtnCopy_Click"/>
            <TextBlock/>
            <TextBlock x:Uid="Info"/>
         </StackPanel>
    </Grid>
</Page>
