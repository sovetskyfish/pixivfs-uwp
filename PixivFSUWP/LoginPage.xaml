<Page
    x:Class="PixivFSUWP.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PixivFSUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Image Source="Assets/LoginBG.jpg" Stretch="UniformToFill"/>
        <Grid Height="460" Width="400"
              HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.Background>
                <AcrylicBrush BackgroundSource="Backdrop" TintOpacity="0.4" TintColor="White" FallbackColor="#99ffffff"/>
            </Grid.Background>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock HorizontalAlignment="Center" Margin="0,150,0,0">让创作变得更有乐趣</TextBlock>
            <Image Source="Assets/PixivText.png"/>
            <StackPanel Grid.Row="1" Margin="20" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <TextBlock Margin="5">用户名</TextBlock>
                <TextBox Margin="5"/>
                <TextBlock Margin="5">密码</TextBlock>
                <PasswordBox Margin="5"/>
            </StackPanel>
            <StackPanel Margin="20" HorizontalAlignment="Stretch" VerticalAlignment="Center" Grid.Row="2">
                <Button x:Name="btnLogin" Background="#25c6ff" Foreground="White" Margin="5,3" HorizontalAlignment="Stretch">登录</Button>
                <Button x:Name="btnReg" Click="BtnReg_Click" Margin="5,3" HorizontalAlignment="Stretch">注册</Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
