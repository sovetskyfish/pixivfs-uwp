<Page
    x:Class="PixivFSUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PixivFSUWP"
    xmlns:viewmodels="using:PixivFSUWP.ViewModels"
    xmlns:controls="using:PixivFSUWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <Page.Resources>
        <DataTemplate x:Key="WaterfallListViewItemTemplate"
                      x:DataType="viewmodels:WaterfallItemViewModel">
            <Grid Height="Auto" Width="Auto" Margin="-12,0" BorderThickness="1.5">
                <Grid.BorderBrush>
                    <RevealBorderBrush Color="Transparent" FallbackColor="LightGray" />
                </Grid.BorderBrush>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition MaxHeight="20"/>
                </Grid.RowDefinitions>
                <Image Grid.RowSpan="2" Stretch="Uniform" Source="{Binding ImageSource}"/>
                <Grid Grid.Row="1" Background="#66ffffff">
                    <TextBlock Text="{Binding Title}"/>
                </Grid>
                <Grid Grid.RowSpan="2">
                    <Grid.Background>
                        <RevealBackgroundBrush Color="Transparent" FallbackColor="Transparent"/>
                    </Grid.Background>
                </Grid>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="DetailContentTemplate" x:DataType="viewmodels:WaterfallItemViewModel">
            <ScrollViewer>
                <StackPanel>
                    <TextBlock
                    Margin="0,8"
                    Style="{ThemeResource TitleTextBlockStyle}"
                    HorizontalAlignment="Left"
                    Text="{x:Bind Title}"/>
                    <Image Source="{Binding LargeImageSource}"/>
                </StackPanel>
            </ScrollViewer>
        </DataTemplate>
    </Page.Resources>

    <Grid x:Name="LayoutRoot">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="AdaptiveStates" CurrentStateChanged="AdaptiveStates_CurrentStateChanged">
                <VisualState x:Name="DefaultState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="700" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MasterColumn.Width" Value="*" />
                        <Setter Target="DetailColumn.Width" Value="0" />
                        <Setter Target="WaterfallListView.SelectionMode" Value="None" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="MasterColumn" Width="350" />
            <ColumnDefinition x:Name="DetailColumn" Width="*" />
        </Grid.ColumnDefinitions>
        <ListView ScrollViewer.VerticalScrollBarVisibility="Hidden"
            x:Name="WaterfallListView" ItemContainerTransitions="{x:Null}"
            ItemTemplate="{StaticResource WaterfallListViewItemTemplate}"
            IsItemClickEnabled="True" ItemClick="WaterfallListView_ItemClick">
            <ListView.Background>
                <AcrylicBrush BackgroundSource="HostBackdrop" TintColor="White" TintOpacity="0.5" FallbackColor="White"/>
            </ListView.Background>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <controls:WaterfallContentPanel TopOffset="80">
                        <controls:WaterfallContentPanel.ChildrenTransitions>
                            <TransitionCollection>
                                <PopupThemeTransition FromVerticalOffset="150"/>
                            </TransitionCollection>
                        </controls:WaterfallContentPanel.ChildrenTransitions>
                    </controls:WaterfallContentPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        <Grid Height="80" VerticalAlignment="Top">
            <Grid.Background>
                <AcrylicBrush BackgroundSource="Backdrop" TintColor="White" TintOpacity="0.5" FallbackColor="White"/>
            </Grid.Background>
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20">为您推荐</TextBlock>
        </Grid>
        <ContentPresenter
            x:Name="DetailContentPresenter"
            Grid.Column="1"
            BorderThickness="1,0,0,0"
            BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
            Content="{x:Bind WaterfallListView.SelectedItem, Mode=OneWay}"
            ContentTemplate="{StaticResource DetailContentTemplate}">
            <ContentPresenter.ContentTransitions>
                <TransitionCollection/>
            </ContentPresenter.ContentTransitions>
        </ContentPresenter>
    </Grid>
</Page>
